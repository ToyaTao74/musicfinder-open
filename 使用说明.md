# MusicFinder 使用说明

一个本地运行的「多平台音乐搜索聚合」小工具，把 QQ音乐 / 酷狗 / 酷我 / 网易云 / 汽水音乐 的数据拉到一起看。

> 本程序**完全本地运行**，不依赖任何外部服务器。数据由各平台官网实时抓取，**必须联网**才能用。

---

## 一、怎么启动

### Windows（exe 版）
1. 把 `MusicFinder.exe` 放到任意文件夹（桌面 / 下载 都行）。
2. 双击 `MusicFinder.exe`。
3. 首次会被 Windows SmartScreen 拦截（"Windows 已保护你的电脑"）→ 点 **「更多信息」** → **「仍要运行」**。这是正常的，因为程序没有买代码签名证书，**不是病毒**。
4. 等几秒，浏览器自动打开 `http://127.0.0.1:5050`（或你自定义的端口）即可使用。

### Mac（app 版）
1. 把 `MusicFinder.app` 拖进"应用程序"文件夹。
2. 双击打开，浏览器自动弹出。

### 开发者模式（源码运行）
```bash
pip install flask openpyxl requests pyinstaller
python app.py
```
浏览器访问 `http://127.0.0.1:5050`。

---

## 二、怎么用

- **首页搜索**：输入歌名（可加歌手） → 搜索 → 同时返回 5 个平台的数据：歌名 / 歌手 / 专辑 / 词曲作者 / 收藏 / 在听 / 评论 / 播放量 / 唱片公司 / 链接。
- **分开的搜索栏**：词曲作者、表演者各有独立搜索框，互不干扰。
- **Excel 批量导入**：首页往下找到"批量"区，上传 `.xlsx`（**第一列是歌名**，每行一首）→ 批量查询，结果可导出。
- **设置页**：点击右上角"设置"进入，可配置各平台登录态 Cookie（见第三节）、测试连接等。

---

## 三、登录态 Cookie（可选，但强烈建议）

部分平台（如网易云、QQ音乐）不登录时只能拿到有限数据。登录后数据更全。

### 保存 Cookie 到本地（重启不丢）
1. 在浏览器里登录对应平台网页版，用开发者工具（F12 → Network / Application → Cookies）复制该平台的 Cookie 字符串。
2. 打开 MusicFinder 设置页 → 对应平台输入框 → 粘贴 Cookie → 点 **「保存」**。
3. Cookie 会写入软件本地文件 `~/.musicfinder/cookies.json`（Mac）或程序数据目录（Windows），**重启程序依然有效**，不用重复填。

> 你这是自用版，Cookie 只存在你本机，不会上传任何地方。

### 关于"浏览器登录"按钮
打包版**没有**内置浏览器自动化（Playwright），所以"浏览器登录"按钮在打包版里是隐藏的。请用上面的「手动粘贴 Cookie」方式。源码开发模式若装了 Playwright 才有该按钮。

---

## 四、自定义端口（避开 5050 冲突）

默认端口是 `5050`。如果这个端口被别的程序占用了，双击程序会打开一个**空白/404 页面**（因为打开的是占用者的服务，不是 MusicFinder）。

解决：让 MusicFinder 换一个端口。

### 方法 A：同目录放 `port.txt`（最省事，推荐）
在 `MusicFinder.exe` 旁边新建一个文本文件，命名为 `port.txt`，内容只写一行端口号，例如：
```
57074
```
双击 `MusicFinder.exe` 即可运行在 `http://127.0.0.1:57074`。

### 方法 B：命令行参数
```
MusicFinder.exe --port 57074
```

### 端口优先级
`--port 参数` > 同目录 `port.txt` > 环境变量 `PORT` > 默认 `5050`

> 端口号范围 **1–65535**，超出会启动失败。

---

## 五、怎么关掉程序（重要）

这个程序是**后台本地服务**，关掉浏览器标签页**不会**退出它。

正确关闭方式：
- **Windows**：`Ctrl + Shift + Esc` 打开任务管理器 → 找 `MusicFinder` → 右键"结束任务"。
- **Mac**：活动监视器搜 `MusicFinder` → 退出；或重启电脑。

---

## 六、常见问题

| 现象 | 原因 / 解决 |
|------|------------|
| 打开是空白页 / 404 | 5050 被别的程序占了 → 见第四节换端口，或重启电脑释放端口 |
| 双击没反应 | 首次 Windows 杀软扫描较慢，稍等；或看任务管理器里有无 MusicFinder 进程、浏览器是否开了对应端口 |
| 浏览器没自动打开 | 手动访问 `http://127.0.0.1:5050`（或你的自定义端口） |
| 提示"未安装 Playwright" | 打包版故意不含 Playwright，用"手动粘贴 Cookie"方式登录即可，不影响搜索 |
| 搜索没数据 | 检查是否联网；部分平台未登录 Cookie 数据有限 |
| SmartScreen 拦截 | 点"更多信息"→"仍要运行"，属正常未签名提示 |

---

## 七、各平台数据说明

- **QQ音乐 / 酷狗 / 酷我 / 网易云 / 汽水音乐**：均支持歌名 + 歌手搜索。
- **唱片公司 / 发行方**：酷狗等平台可从歌词版权行解析出录音版权方（如"录音作品及MV版权：XXX"）。
- 各平台字段完整度不同，以界面实际返回为准。

---

*本说明随程序一同分发，程序首次运行会自动把它复制到 exe/app 同目录。*
